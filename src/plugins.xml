<?xml version="1.0" encoding="utf-8"?>
<plugins>
    <plugin>active-directory</plugin>
    <plugin>analysis-core</plugin>
    <plugin>antisamy-markup-formatter</plugin>
    <plugin>apache-httpcomponents-client-4-api</plugin>
    <plugin>build-failure-analyzer</plugin>
    <plugin>build-name-setter</plugin>
    <plugin>build-timeout</plugin>
    <plugin>build-user-vars-plugin</plugin>
    <plugin>cloudbees-folder</plugin>
    <plugin>cobertura</plugin>
    <plugin>credentials</plugin>
    <plugin>display-url-api</plugin>
    <plugin>durable-task</plugin>
    <plugin>email-ext</plugin>
    <plugin>embeddable-build-status</plugin>
    <plugin>git-client</plugin>
    <plugin>git</plugin>
    <plugin>gravatar</plugin>
    <plugin>greenballs</plugin>
    <plugin>hashicorp-vault-plugin</plugin>
    <plugin>icon-shim</plugin>
    <plugin>jackson2-api</plugin>
    <plugin>javadoc</plugin>
    <plugin>job-dsl</plugin>
    <plugin>junit</plugin>
    <plugin>jsch</plugin>
    <plugin>mailer</plugin>
    <plugin>matrix-auth</plugin>
    <plugin>matrix-project</plugin>
    <plugin>maven-plugin</plugin>
    <plugin>metrics</plugin>
    <plugin>msbuild</plugin>
    <plugin>nomad</plugin>
    <plugin>nunit</plugin>
    <plugin>powershell</plugin>
    <plugin>PrioritySorter</plugin>
    <plugin>rabbitmq-build-trigger</plugin>
    <plugin>rabbitmq-consumer</plugin>
    <plugin>rebuild</plugin>
    <plugin>resource-disposer</plugin>
    <plugin>role-strategy</plugin>
    <plugin>scm-api</plugin>
    <plugin>script-security</plugin>
    <plugin>short-workspace-path</plugin>
    <plugin>ssh-credentials</plugin>
    <plugin>statistics-gatherer</plugin>
    <plugin>structs</plugin>
    <plugin>swarm</plugin>
    <plugin>token-macro</plugin>
    <plugin>violations</plugin>
    <plugin>warnings</plugin>
    <plugin>workflow-api</plugin>,
    <plugin>workflow-durable-task-step</plugin>
    <plugin>workflow-job</plugin>
    <plugin>workflow-scm-step</plugin>
    <plugin>workflow-step-api</plugin>
    <plugin>workflow-support</plugin>
    <plugin>ws-cleanup</plugin>
</plugins>
